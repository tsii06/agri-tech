generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Ancrage {
  id            Int    @id @default(autoincrement())
  txHash        String @unique
  refExpedition String @unique
}
